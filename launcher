#!/usr/bin/bash
source $HOME/.config/panel/config
j4-dmenu-desktop --term=urxvt --dmenu="dmenu -i -h $height -w $(($width-10)) -x $(($x+5)) -y $y -dc '$back_color' -fn '$font_1' -nb '$dnsel_back' -nf '$dnsel_fore' -sb '$dsel_back' -sf '$dsel_fore'"
